drop trigger if exists calcula_pontos_delete;

delimiter $$
CREATE TRIGGER calcula_pontos_delete
AFTER delete ON titulo_professors
FOR EACH ROW
BEGIN
if old.status = 1 then
	UPDATE professors SET total_titulacao = (total_titulacao - old.valor) where id = old.professor_id;
end if;

 END$$
delimiter ;




